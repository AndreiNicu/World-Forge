# CLAUDE.md

> Standing context for AI coding agents (Claude Code, Antigravity, similar) working on this repository. This file is read first on every session start. It tells you what this project is, what the load-bearing architectural decisions are, and what rules govern safe modification of the pipeline files.

---

## What this repository is

World Forge is a **multi-agent pipeline for building immersive roleplay worlds for SillyTavern**. It is not application code in the traditional sense — it is a curated collection of agent specifications (markdown), templates (markdown and JSON), and orchestration logic (markdown) that together define a structured creative writing pipeline.

The pipeline is designed to run inside an agentic VS Code extension (typically Kilo Code; the formerly-recommended Roo Code was retired May 15, 2026). When invoked, it walks a user from a raw idea through 5+ phases of structured drafting and validation, producing a complete SillyTavern-ready world package.

**The repository is the pipeline itself.** Editing files here changes how the pipeline behaves on its next run. There is no compilation step, no test suite, no deployment — files are read directly by the runtime agent.

---

## What this repository is NOT

- **Not a SillyTavern fork or extension.** SillyTavern is the runtime environment that consumes the pipeline's outputs. This repo produces files for SillyTavern; it does not modify SillyTavern itself.
- **Not a runtime engine.** The pipeline does not execute — it is consumed by an agentic IDE extension (Kilo Code) that orchestrates LLM calls. Do not add execution logic, build steps, or deployment configuration.
- **Not a code project.** The "code" here is markdown agent specifications. Treat them as you would treat carefully-versioned prose documents, not as source code.
- **Not for editing during pipeline runs.** When a user is actively running `/worldforge start`, the pipeline files are being read by the runtime agent. Editing them mid-run produces undefined behavior.

---

## Repository structure (authoritative)

```
World-Forge/
├── README.md                         ← Human-facing project description
├── tutorial.md                       ← Usage tutorial
├── AGENTS.md                         ← Standing instructions for agentic tools (Kilo/Cline); routes run-vs-maintenance sessions
├── Notes_On_functionality.md         ← Authoritative reference: how SillyTavern works internally
├── Notes_Quick_Reference.md          ← DERIVED ~5KB distillation of Notes (position enum, flags, assembly); agents consult it first
├── LICENSE
├── .gitignore
├── .kilocodeignore                   ← Keeps Samples/ + maintenance docs out of runtime agent context
├── .kilo/
│   └── kilo.jsonc                    ← Preconfigured Kilo Code per-phase agents (OpenRouter DeepSeek flavor; auto-loaded)
├── Kilo_Variants/                    ← Drop-in all-seats kilo.jsonc alternates (copy over .kilo/kilo.jsonc to use; NOT auto-loaded)
│   ├── README.md
│   ├── kilo.glm-5.2.jsonc            ← All seats GLM 5.2, reasoning effort high, no temperatures, Z.AI upstream hard-pinned
│   └── kilo.kimi-k2.5.jsonc          ← All seats Kimi K2.5 (general-line, chat-tuned; per-phase temperatures kept)
├── contracts/                        ← CANONICAL source of truth for the design contracts shared with the SillyTavern fork's world-forge + npc-memory extensions (the fork mirrors these read-only and CI-checks byte-equality)
│   ├── README.md                     ← What lives here + the canonical/mirror relationship
│   ├── MEMORY_CONTRACT.md            ← npc-memory data channel ([[NPC_MANIFEST]], facets, stable ids, turn tag, scenes)
│   └── WORLD_FORGE_SYNC.md           ← Runtime seams: Director-card tag (§2), narration-surface aliases (§3), </style_contract> marker (§4), [[WORLD_CALENDAR]] Scene Tracker date seed (§5), style_override runtime
├── tools/
│   ├── validate_export.py            ← Read-only Export/ JSON validator (explicit exception to the no-code rule; see Out of scope)
│   └── validate_pipeline_state.py    ← Read-only process-state validator: ledger claims vs. artifacts on disk (second sanctioned exception; see Out of scope)
├── agent_roles/                      ← Phase-specific agent specifications (each opens with a 📂 CONTEXT MANIFEST)
│   ├── 00_The_Interviewer.md
│   ├── 01_The_Refiner.md
│   ├── 02_The_Architect.md
│   ├── 03_The_Editor.md
│   ├── 03b_The_Voice_Auditor.md
│   ├── 03c_The_Arc_Transition_Auditor.md
│   ├── 03d_The_Intimacy_Auditor.md
│   ├── 04_The_Compiler.md
│   ├── 05_The_Prompt_Engineer.md
│   ├── 05a_Block_Library.md          ← Preset block library (split from 05; loaded only for preset authoring/resync)
│   ├── 06_The_Intimacy_Architect.md
│   ├── SHARED_Style_Contract_Reference.md
│   ├── Brainstormer/                 ← Optional ideation agent (upstream of Phase 0; divergent premise-finding, writes informal Brainstorm_Notes.md, never a World Seed)
│   │   └── 00_The_Brainstormer.md
│   ├── Converter/                    ← Convert-pipeline agent (reframe a shipped world into a new build)
│   │   └── 00_The_Converter.md
│   ├── Auditioner/                    ← On-demand behavioral probe (post-launch, read-only; single-scenario cousin of the Voice Auditor)
│   │   └── 00_The_Auditioner.md
│   └── revise/                       ← Revision-pipeline mini-agents (surgical post-launch edits)
│       ├── 00_The_Reviser.md
│       ├── 01_The_Refiner_mini.md
│       ├── 02_The_Architect_mini.md
│       ├── 02b_The_Intimacy_Architect_mini.md
│       ├── 03_The_Editor_mini.md
│       ├── 03b_The_Voice_Auditor_mini.md
│       ├── 03c_The_Arc_Transition_Auditor_mini.md
│       ├── 03d_The_Intimacy_Auditor_mini.md
│       ├── 04_The_Compiler_mini.md
│       └── 05_The_Prompt_Engineer_mini.md
├── templates/                        ← Structural references the pipeline produces output against
│   ├── World_Seed_Template.md        ← Phase 0 authoring guide
│   ├── Char_Card_creation.md         ← Card field-by-field authoring guide
│   ├── Lorebook_creation.md          ← Lorebook entry authoring guide
│   ├── User_Persona_template.md      ← {{user}} persona description (User.md) authoring guide
│   ├── Convert_Brief_Template.md     ← Convert pipeline input (preservation matrix as a fillable brief)
│   ├── char_template.json            ← Raw V3 character-card JSON schema exemplar
│   ├── Lorebook_Template.json        ← Raw lorebook JSON schema exemplar
│   └── Chat_Completion_Preset_template.json   ← Preset JSON structure (Prompt Engineer target)
├── workflows/                        ← Pipeline orchestration
│   ├── world-forge.md                ← Router: pipeline map, world mode, state ledger, checkpoint discipline, DISPATCH PROTOCOL, trigger commands; dispatches the four build stage files
│   ├── world-forge-discovery.md      ← Stage 1: Brainstorm pre-phase + Phases 0–1 (Interviewer, Refiner)
│   ├── world-forge-drafting.md       ← Stage 2: Phases 2–2.5 (Architect, Intimacy Architect)
│   ├── world-forge-validation.md     ← Stage 3: Phases 3–3.7 (Editor + auditors, iterative loop)
│   ├── world-forge-construction.md   ← Stage 4: Phases 4–5.5 (Compiler, Prompt Engineer, manual apply)
│   ├── world-forge-postlaunch.md     ← Post-launch operations on shipped worlds: revise routing, CONVERT summary, PRESET RESYNC, AUDITION
│   ├── world-forge-revise.md         ← Revision fork (surgical post-launch edits, Phase R0–R5.5)
│   └── world-forge-convert.md        ← Convert fork (reframe a shipped world into a new build, Phase C0)
├── wiki/                             ← Setup & tooling guides (linked from README/tutorial)
│   ├── README.md                     ← Wiki index
│   ├── Agentic-Tools-and-Models.md   ← Kilo/Cline + model comparison (incl. Roo Code retirement note)
│   └── Kilo-Code-Setup.md            ← Dedicated Kilo Code setup walkthrough
└── Samples/                          ← Example world outputs for reference
```

**File authority levels:**
- **READ-ONLY for the user's project work** but writable here in the pipeline repo: all `agent_roles/`, `templates/`, `workflows/`, `tools/`, `Notes_On_functionality.md`, and `Notes_Quick_Reference.md`. These are the pipeline definition.
- **Generated at runtime** in the user's project folder (NOT in this repo): `Drafts/`, `Export/`, `World_Seed.md`, audit reports. These never appear in this repo unless they are explicit samples in `Samples/`.

---

## Core architectural principles (load-bearing — DO NOT VIOLATE without explicit instruction)

These principles took multiple iterations to converge on. Each one has a specific failure mode it prevents. Changes that violate them will produce subtle bugs that surface only at runtime in roleplay sessions.

### 1. The Three-Tier Lorebook Architecture

Every piece of world information belongs to exactly one tier:

- **Tier 1 — World Lorebook** (permanent, arc-agnostic): world rules, factions, species, mechanics, standing locations. Position 0 (Before Char Def). Never replaced or disabled.
- **Tier 2 — Character Lorebooks** (permanent, arc-agnostic, one per major character): physical description, psychological dimensions, relationships, history. Position 1 (After Char Def). Never replaced.
- **Tier 3 — Arc Lorebooks** (modular, one per arc, swapped in/out): ARC_STATE, CHARACTER_STATE, NPC_SHIFT, DRAMATIC_BEAT, TENSION, hidden information rules. Only one arc lorebook active at a time.

If you find content that would fit in multiple tiers, this is a design smell. Surface it for review — the right home is usually the most modular tier (Tier 3 if it changes per-arc, Tier 2 if it changes per-character, Tier 1 only if it's truly universal).

### 2. The Override Architecture (paired contract)

Character cards' `system_prompt` and `post_history_instructions` fields override the SillyTavern Chat Completion Preset's Main Prompt and Jailbreak blocks at runtime, UNLESS the card uses the `{{original}}` macro to splice the preset's content back in.

**Mandate:** Every character card's `system_prompt` and `post_history_instructions` MUST begin with `{{original}}` on its own line, followed by character-specific content.

**The division of labor:**
- **Preset Main Prompt** = engine-only (world-agnostic, character-agnostic): prose style, narration discipline, perspective rules, formatting rules, generic creative framework
- **Card `system_prompt`** = character-specific only: identity, behavioral mandates, prohibitions, trigger-response pairs

**Forbidden in cards (Editor will hard-fail):** narration rules, formatting rules, perspective rules, style guidelines, generic creative framework statements, generic character embodiment principles. These are engine-level and live in the preset.

**Forbidden in preset Main Prompt (Prompt Engineer Pass 1 will hard-fail):** character names, arc names, faction names, location names, character-specific psychology language. These are character/world-specific and live in cards/lorebooks.

If you find yourself moving content between cards and preset, verify which side of the contract it belongs on first.

### 3. Audit-vs-Apply Separation

The Editor (Phase 3), Voice Auditor (3.5), Arc Transition Auditor (3.6), Intimacy Auditor (3.7), and Prompt Engineer (5) are **read-only on draft and export files**. They produce critique reports, audit reports, and recommendations — they do not modify the files they audit.

The Architect (Phase 2) and Intimacy Architect (Phase 2.5) are the **only agents with write authority on drafts**. The Compiler (Phase 4) is the only agent with write authority on `Export/` JSON files — with one narrow, post-launch exception: the Prompt Engineer gains write authority on the single `Export/[WorldName]_ChatPreset.json` file (and only that file) under Preset Resync Mode, and the mini-Prompt-Engineer may apply three narrow preset toggles under the revise pipeline (Multi-Character Dynamics, NSFW, and the Style Contract's conditional ACTIVE-SPEAKER / DIRECTOR-CARD RULE lines). See principle #8. Both exceptions are preset-only; neither touches lorebook or card JSON.

The Prompt Engineer's audit recommendations (Sections 7 and 8 of `Prompt_Engineer_Audit.md`) require **manual user application** — see Phase 5.5 in `workflows/world-forge-construction.md`. This is intentional: it preserves audit reviewability and prevents self-validating corrections.

When modifying any agent spec, do not change its read/write authority without explicit instruction. The asymmetry is load-bearing.

### 4. Position Rationale Requirement

Every lorebook entry produced by the pipeline must include a `Position Rationale:` field. Default positions (per tier and entry type, documented in `agent_roles/02_The_Architect.md` Section 6) get the literal value "DEFAULT". Non-default positions require a one-sentence justification referencing `Notes_On_functionality.md` and explaining why the default fails.

The Editor (Step 4.5) hard-fails entries with missing or shallow rationales. When editing entry templates, preserve the Position Rationale field.

### 5. ARC_STATE Two-Subsection Structure

ARC_STATE entries (one per arc, fires constantly during the active arc) MUST contain two clearly-labeled subsections:

- `**Dramatic Situation:**` — descriptive scene-setting, world-fact register
- `**Tonal Mandate (binding behavioral directive — applies to every response in this arc):**` — directive bullets using imperative language (resist, dominates, never default to, dwells on, elides, do not, must, never, always)

The Tonal Mandate must contain 4-8 bullets covering active register, prose dwells on, prose elides, live scene types, activity cadence (when the arc has active principal NPCs), and hard prohibitions (where relevant). The Editor (Step 4a) hard-fails entries missing the structure or with insufficient directive language. The **activity cadence** directive (Editor Step 4a-3b) is what mechanizes NPC agency: principal NPCs have a Tier 2 §7.D **Standing Goal** (an active objective + pursuit moves), and the cadence directive instructs the model to have a present or off-screen NPC advance its goal when a scene lulls rather than freezing to wait on `{{user}}`. A subplot-shaped Standing Goal may optionally be staged as an **Escalation Ladder** (§7.D): 2–4 ordered stages with in-fiction observable advance conditions, a stated endpoint, and a stated `{{user}}` collision; the ladder's stages are permanent Tier 2 content, the *active stage* is Tier 3 state (named per-arc in the NPC_SHIFT stage line and by the cadence bullet, which also binds the progression discipline — advance only on stated condition, never skip, never self-resolve). The Editor's Step 4a-3c hard-fails malformed ladders and stage-less cadence bullets. The Voice Auditor's Step 3J tests that NPCs actually take that initiative and that their moves trace to a stated goal (for laddered NPCs, to the named active stage, with a stage-discipline probe). This is the arc-mode counterpart of the sandbox aliveness contract (#9); the generic engine half lives preset-side in the `npc_ensemble` block, the world-specific goals in Tier 3.

This structure exists because un-split ARC_STATE entries get interpreted as world-description rather than as binding directives, causing the model to default to its own tonal disposition rather than the arc's specified register.

### 6. Revision Pipeline (Post-Launch Surgical Edits)

Once a world has shipped (Phase 5.5 complete), surgical changes use the **revision pipeline** in `workflows/world-forge-revise.md` — a parallel fork running mini-agents under `agent_roles/revise/`. The minis are smaller, scope-locked counterparts of their initial-build parents.

**The bright line:** any revision that requires changes to Master Design Section 1 (Core Concept & Tone) or Section 11a (Style Contract world defaults) is **out of scope for the revision pipeline**. The Reviser (Phase R0) classifies and bounces these to a full pipeline re-run via `/worldforge skip phase0`, reusing the existing `World_Seed.md`. Per-card overrides in Section 11b stay in revision scope; only world-default changes bounce. **`World Mode` (arc | sandbox) lives in Section 1, so flipping it is a bright-line bounce** — the revise pipeline edits a world *within* its mode; converting arc↔sandbox is a full rebuild. When a world's *accumulated* revisions outgrow surgical editing (markers everywhere, seed badly stale, structural changes coming), the consolidation path is the Convert pipeline's **Rebaseline mode** (#10), not another revision. When the user can't even *name* the concern — something feels off in play but they can't point at it — `/worldforge revise --brainstorm` runs the Brainstormer's **revision-diagnostic posture** (#11) upstream of the Reviser to locate it divergently (reading `Master_Design.md` read-only, offering to fold in parked ideas from a standing `Big_Brain_Storm.md` if one exists, and writing a primary concern to `Brainstorm_Notes.md`); the Reviser then classifies that concern as normal. The Brainstormer enforces the same bright line and flags an out-of-scope diagnosis as a bounce, never a revision.

**Sandbox-aware.** The revise pipeline handles `World Mode: sandbox` worlds through the same minis. The Reviser reads `World Mode` and uses three `sandbox_*` scope types (`sandbox_state_recalibration`, `sandbox_entry_modify`, `sandbox_entry_add`) in place of the `tier3_arc_*` types; the NPC and intimacy scopes are mode-aware (principal/roster NPCs, the single standing `Sandbox_Intimacy_Register`). The Arc Transition Auditor (R3.6) never fires on a sandbox world. Because every parent agent is already sandbox-aware (#9), the minis inherit it — they document only thin sandbox deltas.

**Load-bearing properties of the revise pipeline:**
- **Cascade scope.** Mini-agents touch only files listed in the Revision Log entry's confirmed cascade. Silent scope expansion is forbidden — cross-references the user did not flag are surfaced for confirmation, not silently widened.
- **UID preservation.** The mini-Compiler reads existing Export/ JSON before rewriting, preserves UIDs on unchanged entries, assigns next-free UIDs to new entries, and never deletes unless cascade explicitly says so. This keeps running SillyTavern chat states viable across revisions.
- **Inline revision markers.** Every change site in Master_Design.md and Drafts/ gets a `<!-- REVISED IN R[N] -->` or `<!-- CREATED IN R[N] -->` marker. These are the canonical audit trail at the change site; the per-revision report in `Drafts/Revision_R[N]_Report.md` is the durable detail. **A marker annotates the single current version of a passage — it is not a license to stack.** An in-place `*_modify` / `*_calibration` edit *replaces* its target where it sits (mini-Refiner R1.5, mini-Architect Foundational Rule 8); leaving the old text and adding a reworded copy beneath it produces near-duplicate "stacks" that compound across revisions and reach the runtime prompt as conflicting copies. The mini-Editor's **Step R3.1b** hard-fails stacked/duplicated content; append mode is reserved for genuinely new content, never a reworded version of existing material. On the Export/ side, where filenames can't be renamed and JSON can't carry extra fields, the mini-Compiler maintains `Export/REVISED_FILES.md` — a cumulative manifest of every export file ever touched by a revision (file, latest revision, date, summary). It is the sole revision marker on the Export side; the markers themselves are stripped on the Drafts→Export transcription (mini-Compiler Foundational Rule 9, backstopped by `tools/validate_export.py`).
- **Audit/apply separation preserved.** The mini-Editor and mini-auditors are read-only on drafts; the mini-Prompt-Engineer is read-only on Export/ JSON except for the Chat Completion Preset, which it may modify only under narrow triggers (Multi-Character Dynamics block toggle, NSFW block toggle, Style Contract conditional-line toggles — ACTIVE-SPEAKER RULE / DIRECTOR-CARD RULE).
- **Single revision at a time.** A revise invocation captures one logical concern. Multiple unrelated changes run as separate sequential revisions; overlapping PENDING entries are forbidden.

When editing any `agent_roles/revise/*` file, the parent agent's foundational rules still apply — the mini documents only the deltas. Cross-reference the parent before adding rules that should already inherit.

### 7. Notes_On_functionality.md is the SillyTavern source of truth

This file is the authoritative reference for SillyTavern's runtime behavior — prompt assembly, world info scanning, position values, lorebook flag effects, group chat field combination. When editing pipeline files, if you need to reference SillyTavern behavior, consult this file. If a position value, flag effect, or runtime mechanic appears to be wrong in this file, verify against the official SillyTavern source (https://github.com/SillyTavern/SillyTavern) before editing — this file has been corrected before for outdated content, and getting it right matters because every agent consults it.

**Authoritative position enum (line ~661 of Notes_On_functionality.md):**
- 0: before (Before Char)
- 1: after (After Char)
- 2: ANTop (Author's Note Top)
- 3: ANBottom (Author's Note Bottom)
- 4: atDepth
- 5: EMTop (Example Messages Top — prepended to dialogueExamples)
- 6: EMBottom (Example Messages Bottom — appended to dialogueExamples)
- 7: outlet

### 8. Preset Resync (Post-Launch Preset Refresh)

There are three distinct operating modes, not two: the **initial build** (`/worldforge start`, Phases 0–5.5), the **revision pipeline** (`/worldforge revise`, principle #6), and **preset resync** (`/worldforge resync-preset`). Resync exists because a shipped world's `Export/[WorldName]_ChatPreset.json` can fall behind in two independent ways: the pipeline's preset spec evolves (a reframed core block, a new optional block, a changed template flag), and/or the world's content changes through the revise pipeline in ways that surface inside preset blocks (Deep Think names the arcs, the multi-character lattice names characters) but that the revise mini-Prompt-Engineer never writes.

It invokes the Prompt Engineer in **Preset Resync Mode** (`agent_roles/05_The_Prompt_Engineer.md` Section 8). The agent re-derives each block's content from the current `templates/Chat_Completion_Preset_template.json` + block library (`agent_roles/05a_Block_Library.md`) + the post-revision `Drafts/Master_Design.md`; writes only the blocks whose content has drifted and adds newly-warranted optional blocks; **preserves** block identifiers, `prompt_order`, revision-applied toggles, and the user's field-level customizations; re-runs the Section 5f Pass 1 + Pass 2 self-validation; and writes `Export/Preset_Resync_Report.md`.

**Load-bearing properties:**
- **Preset-only write authority.** This is the one place the Prompt Engineer writes to `Export/` (see principle #3). It touches the Chat Completion Preset and nothing else — no lorebook or card JSON.
- **Makes no content changes.** Resync reflects already-applied content and spec changes into the preset. The revise pipeline *makes* surgical content changes; resync makes none. A world can be resynced without ever entering revise, and revised worlds can be resynced afterward.
- **Distinct from `resume phase5`.** `resume phase5` re-runs the full Phase-5 audit during an in-progress build; resync is a maintenance op on an already-shipped world that only refreshes the preset.
- **Low risk.** A Chat Completion Preset is a global SillyTavern settings profile, not UID-bearing world info, so re-importing a resynced preset does not disturb running chat states. Git is the rollback path.

See the **PRESET RESYNC** section of `workflows/world-forge-postlaunch.md` for the full operation.

### 9. World Mode: Arc vs. Sandbox

A world is built in one of two **modes**, declared in World Seed Section 1 (`World Mode: arc | sandbox`) and recorded by the Refiner. `arc` is the default and the legacy behavior — every existing world is an arc world, and nothing about arc worlds changes. `sandbox` exists for worlds that have **no narrative arc**: open-ended power-fantasy / world-director / life-sim worlds where the experience is anchored by a standing world-state rather than a progression of arcs with entry/exit triggers and dramatic beats.

The mode is a branch *through* the existing pipeline, **not a parallel fork.** The same Interviewer, Refiner, Architect, Editor, Voice Auditor, Compiler, and Prompt Engineer run; sandbox mode swaps the Tier 3 spine and the large-cast NPC format. `/worldforge start --sandbox` is a convenience that pre-sets the Interviewer; the World Seed field is the source of truth (so a hand-written seed or a `skip phase0` run carries the signal).

**What sandbox mode changes (load-bearing):**
- **Tier 3 becomes one always-active Sandbox lorebook**, not *N* swappable arc lorebooks. It contains a `SANDBOX_STATE` entry (the `ARC_STATE` analog — same constant / `ignoreBudget` / position-1 mechanics and the same mandatory two-subsection structure from principle #5, with `**Standing Situation:**` replacing `**Dramatic Situation:**`) and at least one `WORLD_PULSE` entry (the `TENSION` analog at position 4 — a sustained "the world is alive and reactive" directive rather than a stakes countdown). Standing `LOCATION` entries as needed. There are no `CHARACTER_STATE`, `NPC_SHIFT`, `DRAMATIC_BEAT`, or arc-trigger entries, because there is no arc.
- **The SANDBOX_STATE Tonal Mandate carries the aliveness contract**: NPCs pursue their own agendas, initiate scenes, and carry off-screen continuity; the world reacts to and remembers `{{user}}`'s actions; it never freezes waiting for `{{user}}`. This is what anchors tone when no arc is carrying it. The contract is made concrete by per-NPC **Standing Goals** (Architect §7.D): each principal NPC carries an active objective + pursuit moves, and the aliveness directive instructs the model to have an NPC advance its goal when a scene lulls. Laddered goals (the optional §7.D Escalation Ladder, #5) work in sandbox too — but with soft state: the active stage is named in the SANDBOX_STATE aliveness bullet and anchored by WORLD_PULSE's depth-2–4 recency injection (there is no swappable arc state to carry it), with the revise pipeline's `sandbox_state_recalibration` as the manual ratchet when play moves past the named stage. The Voice Auditor's Step 3J tests this (initiative + goal-trace, plus stage-trace/discipline for laddered NPCs) in both modes. The same mechanic runs in arc mode via the ARC_STATE activity-cadence directive (#5) — it is mode-agnostic because NPCs exist in both modes.
- **Large casts use a two-tier NPC model** (Architect §7): a few **principal NPCs** keep the full §7.D profile; the rest are **roster NPCs** authored as a compact, fixed-schema stat block engineered for differentiation at scale. The binding rule: every roster NPC's **voice fingerprint must be unique across the roster.** This is the structural defense against the homogenization that swallows large NPC casts in sandbox play.
- **The Voice Auditor adds a distinctiveness lens** (sandbox-only Step 3I): a blind-line test across the roster that flags any two NPCs whose voices could be swapped. The arc-register checks reframe to "standing register vs. `SANDBOX_STATE`."
- **Phase 3.6 (Arc Transition Auditor) is skipped** in sandbox mode — there are no arc seams to audit, exactly as Phase 3.7 is skipped without intimacy. Cross-arc consistency qualifiers and the ≥8-entries-per-arc floor do not apply.
- **The Prompt Engineer defaults sandbox presets** to enabling Multi-Character Dynamics, including the optional **NPC Ensemble & Enrichment** block (`npc_ensemble`: NPC-to-NPC dialogue, ensemble prose scaling, and organic NPC enrichment within guardrails — the NPC-roster analogue of the `enhanceDefinitions` enhancer), and weighting Sensory Embodiment high. These are runtime engine directives in the preset, so they live on the preset side of the override contract (#2) — never in cards.

**Boundaries:** Sandbox mode does not touch SillyTavern, the override architecture (#2), audit/apply separation (#3), Position Rationale (#4), or the preset contract — it only repoints what Tier 3 contains and how a large NPC cast is authored. Intimacy (Section 8) stays orthogonal but is mode- and cast-aware: a sandbox world folds its `INTIMACY_FUNCTION` register into a single standing `Sandbox_Intimacy_Register` (no per-arc registers), and **NPC intimacy follows the principal/roster split** — principal NPCs get full Tier 2 Intimacy Profiles, roster NPCs get compact §6.5 intimate stat blocks with the same uniqueness rule (no two NPCs interchangeable in an intimate scene). The Intimacy Auditor checks NPC intimate coverage and distinctiveness (Step 3H). The **revise pipeline is sandbox-aware** (see #6): sandbox worlds are revised through the same minis using the `sandbox_*` scope types, with R3.6 never firing. *Flipping* a world between arc and sandbox is a Section 1 `World Mode` change and bounces out of the revise pipeline — but this is one of the canonical cases the **Convert pipeline** (#10) is built for, alongside protagonist swaps and Style Contract changes.

See the **WORLD MODE: ARC vs. SANDBOX** section of `workflows/world-forge.md` for the full operation.

### 10. Convert Pipeline (Reframe + Rebaseline)

There is a fourth operating mode alongside initial-build / revise / preset-resync: **`/worldforge convert`** (`workflows/world-forge-convert.md`). It reframes a shipped world into a new build with a different protagonist, World Mode, Core Concept, or Style Contract world defaults — exactly the change-categories principle #6 explicitly bounces out of revise. The Converter (`agent_roles/Converter/00_The_Converter.md`) is a single phase (C0): it reads the source's `Master_Design.md` (read-only), captures the user's keep/modify/regenerate decisions per source section (interview or pre-authored Convert Brief), and writes a new `World_Seed.md` to a target project folder. The user then runs `/worldforge skip phase0` against the target; the standard pipeline (Phases 1–5.5) builds the new world end-to-end.

The Converter has two operating modes. **Reframe** (default) is the above — something structural changes. **Rebaseline** (`--rebaseline`; Converter spec Section 9) is the zero-axes-replaced conversion: same world, same protagonist, rebuilt clean at its *post-revision* state — the consolidation path for a world whose accumulated revisions (#6) have outgrown surgical editing, optionally folding in new mechanics at seed level. The consolidation is **seed-anchored** (Step D): the source `World_Seed.md` carries **1:1** as the base (untouched sections keep the user's original wording verbatim, unmarked), each applied revision is written into the affected seed passage in place (replace-not-stack, at seed grade), a **divergence scan** surfaces post-seed content no revision explains (Phase 1+ elicitations) for ask-first fold-in — never silent in either direction — and the structure upgrades to the current seed template without re-wording carried content; distillation from the Master Design runs only on the explicit **`--distill`** flag (deliberate opt-out when the original seed is thin or outgrown) or as the announced fallback for a source with no seed file (confirming that announcement counts as passing the flag) — never as a silent default while a source seed exists. Rebaseline inverts the always-regenerate rules below (their premise — a changed protagonist — is absent): Sections 3 / 5 / 7b, intimate functions, per-card overrides, relationship-to-`{{user}}` content, and the four Section 4 strip rules all flip to keep/carry. Revision content carries; `<!-- REVISED IN R[N] -->` markers do not (provenance moves to `<!-- REBASELINED FROM ... -->` comments at change sites + a manifest high-water mark). `Drafts/Revision_R*.md` + `Export/REVISED_FILES.md` become required reading with a source-integrity check (every reported revision must be visible in the Master Design — halt on drift). The honest cost is stated explicitly: the rebuild compiles **fresh UIDs, so running SillyTavern chats do not migrate** — revise preserves UIDs precisely to avoid this; rebaseline trades it for cleanliness. When the consolidation is a staging step for bigger changes, **`--then-interview`** (Converter Section 9 Step H) chains the hand-off into Phase 0 instead of `skip phase0`: the Interviewer's **seed-revision posture** (`agent_roles/00_The_Interviewer.md` Section 9) interviews only the user's changes against the consolidated seed — full Phase 0 depth on the delta, cascade re-elicited on coupled fields, untouched sections left alone — then signs off and hands to Phase 1 as normal. **`--then-brainstorm`** (same Step H) inserts the **Brainstormer's improvement posture** (`agent_roles/Brainstormer/00_The_Brainstormer.md` Section 8, #11) ahead of that Interviewer — for when the user wants to change the consolidated world but hasn't decided what: the Brainstormer diverges on improvement directions, writes informal `Brainstorm_Notes.md` (never the seed), and the seed-revision Interviewer reads those notes as proposals. It requires `--rebaseline` and supersedes `--then-interview`. The chain is also the rebaseline-side pickup point for the standing `Big_Brain_Storm.md` idea file (#11): the chained Brainstormer offers the source's parked ideas ask-first (its Section 8 step 1a) and, when brought in, carries the curated file forward to the target project; a plain `--rebaseline` (Converter Section 9 Step F) surfaces the file's existence — never reading it — and routes (fold via the chain, state an idea directly as new mechanics, or leave it parked), recording the disposition in the manifest.

**Load-bearing properties:**
- **Read-only on the source project.** The Converter never modifies any file in the source folder. Period.
- **Write-only on the target's `World_Seed.md`.** Does not write `Drafts/`, `Export/`, `Master_Design.md`, or any other target file. The standard pipeline produces those. Convert is upstream of the standard pipeline, not parallel to it — no downstream agent needs special handling for a converted world; the new seed and Master Design are first-class Phase 0/1 outputs.
- **Overlap floor refusal (the reskin refusal).** If the conversion replaces three or four of (setting, protagonist, factions, tone), the Converter refuses with an explicit bounce to `/worldforge start`. Borderline (two replaced) gets surfaced for explicit user confirmation. **Pure reskin is intentionally out of scope** — at three-axes-replaced, the source is creative reference, not a structural source. In Rebaseline mode the floor inverts into the **zero-axes gate**: any replaced axis reclassifies the run as a reframe conversion (announced, not refused).
- **Always-regenerated content (reframe mode).** Section 3 (`{{user}}`), Section 5 (arcs / Sandbox Charter), Section 7b (test scenarios), per-arc / standing intimate functions, and per-card style overrides are always regenerated downstream — they are protagonist-shaped or downstream-derived. The Converter does not let the user mark them `keep` — except in Rebaseline mode, where they default to keep.
- **Role reassignment surfacing.** Five canonical cases (old protagonist → NPC; source NPC → new protagonist; power-tier shift on `{{user}}`; character dropped; role shift not involving the protagonist) are surfaced explicitly to the user and confirmed before the seed is written. Source content carries forward where it can; relationship-to-`{{user}}` content is always reauthored downstream and gets a marker comment in the seed.
- **Single source.** No mashups. The Converter accepts exactly one source world per conversion.
- **Conversion Manifest at top of seed.** Records source, intent (verbatim), overlap floor classification, per-section preservation decisions, role reassignments, and cross-references the user should be aware of. The Refiner reads this at Phase 1 to route accordingly.

**Boundaries:** Convert does not touch SillyTavern, the override architecture (#2), audit/apply separation (#3), Position Rationale (#4), or any other architectural principle. It is purely an upstream seed-production operation. It is *the* legitimate path for the change-categories revise bounces (Section 1, Section 11a, World Mode flips), preserving structural world-building that `/worldforge start` from scratch would discard.

See the **CONVERT** section of `workflows/world-forge-postlaunch.md` and the full operation in `workflows/world-forge-convert.md`.

### 11. The Brainstormer (optional upstream ideation)

There is an optional step upstream of even Phase 0: **`/worldforge brainstorm`** (`agent_roles/Brainstormer/00_The_Brainstormer.md`). It is the **divergent** counterpart to the convergent Interviewer — for a user who arrives with only a vibe (an image, a mood, a single character) and nothing solid enough for the Interviewer's structured, specificity-demanding questions to land. It generates premise directions, yes-ands the user's instincts, and converges only when an idea has a pulse, then hands off to `/worldforge start`.

**Load-bearing properties:**
- **Writes only its notes files** — informal, unstructured ideation notes in the project folder, explicitly *not* a World Seed: the run-scoped `Brainstorm_Notes.md` (overwritten fresh every run, posture-stamped), plus the standing `Big_Brain_Storm.md` idea file — written on the standalone improvement door, and carried forward (curated) to the target project by the chained improvement door when the user folds it in. The Brainstormer never authors or edits `World_Seed.md`, never writes `Drafts/`/`Export/`, never classifies tiers, and touches none of #1–#5. Seed authorship belongs to the Interviewer alone — this bright line is the whole point.
- **Optional and skippable.** A user with a developed concept skips it. Nothing downstream depends on a brainstorm having happened; the Interviewer reads `Brainstorm_Notes.md` only *if present* (its Context Manifest lists it under "Load if present") and still runs the full interview, pushing for the specificity the Brainstormer deliberately left open and confirming (not inheriting) any World Mode leaning.
- **Four postures.** The default (spec Sections 1–7) is the fresh-fragment start. The **improvement posture** (spec Section 8) brainstorms *changes to an existing world*, diverging on what to add, rework, or deepen — respecting the world's spine (an idea that flips World Mode, swaps the protagonist, or overturns the core concept is flagged as reframe-conversion territory, not an improvement). It has two doors. **Chained**, via the Converter's **`--then-brainstorm`** flag (#10): after a rebaseline consolidates a revised world, the Brainstormer reads the consolidated `World_Seed.md` + Conversion Manifest, explores improvements — offering the source's parked `Big_Brain_Storm.md` ideas ask-first (spec Section 8 step 1a) and carrying the curated file forward to the target when brought in — writes its notes, and the chain continues into the Interviewer's seed-revision posture, which reads those notes as *proposals*. **Standalone**, via **`/worldforge brainstorm --improve`**: no-commitment idea ping-pong against any existing world ("would this fit? should something be added?") — it reads the world's current state read-only (`Drafts/Master_Design.md` when built, `World_Seed.md` otherwise) plus any prior `Big_Brain_Storm.md`, writes/refreshes the standing **`Big_Brain_Storm.md`** idea file (curated fresh each session, still-open ideas carried forward; `Brainstorm_Notes.md` untouched), and dispatches nothing. The organic pickup has two points: a later `revise --brainstorm` run *asks* the user whether to fold the parked ideas into its diagnosis and adapts a chosen one into the revision-diagnostic notes — the Reviser itself never reads `Big_Brain_Storm.md` — and a rebaseline picks them up on the convert side: the `--then-brainstorm` chain's Brainstormer asks the same question and carries the curated file forward to the target, while a plain `--rebaseline` (Converter Section 9 Step F) surfaces the file's existence without reading it and routes. The user can equally take a landed idea straight through a normal door (`/worldforge revise` as fresh intent, seed edits, or `/worldforge convert`), or leave it parked. The **revision-diagnostic posture** (spec Section 9) serves the post-launch *pre-articulation* case — something feels off in a shipped world but the user can't name what to revise. Invoked by `/worldforge revise --brainstorm` (#6), it reads `Drafts/Master_Design.md` read-only, diagnoses divergently (the six domain lenses as its diagnostic vocabulary, the "where does it sag" move as its primary instrument), and writes one primary concern (+ candidate future concerns) to `Brainstorm_Notes.md`; the Reviser reads that as captured intent. It enforces the same Section 1 / 11a / World Mode bright line and bounces an out-of-scope diagnosis rather than dressing it as a revision. The **adaptation posture** (spec Section 10) serves the user who arrives with an *existing narrative document* (a story, a fanfiction, a roleplay log) instead of a fragment. Invoked by `/worldforge brainstorm --adapt <document_path>`, it reads the document read-only and extracts the world latent in it (cast — including each character's **dialogue voice** captured with verbatim sample lines; setting; tone; the **prose style and pacing** as a sample for the Style Contract; and any **intimate register** the source shows — dynamics/explicitness/kinks — as raw material for the Intimacy Architect, Phase 2.5), then diverges on the gap between a story-to-read and a world-to-play-in — above all the **`{{user}}` slot** the document's fixed POV cannot supply, which it *recommends from the document's POV and confirms* rather than picking blind or silently (play as the source's protagonist / a new insert / a promoted side character). It writes an `adaptation`-stamped `Brainstorm_Notes.md` — extracted world + prose sample + intimate register + the `{{user}}` decision + an explicit list of gaps the document didn't fill — that the Interviewer reads exactly like a `fresh-start` file, attributing extracted material to the source and never inventing over a gap (the bright line still holds: it extracts material, it does not author a seed or intimate structures).

**Boundaries:** the Brainstormer is upstream of the pipeline (and, in the revision-diagnostic posture, upstream of the Reviser), not a phase of it — it does not advance the Pipeline State Ledger, invoke downstream agents, or produce any first-class pipeline artifact (`Brainstorm_Notes.md` stays informal in all four postures). It is a front porch. Its one downstream coupling is the revision-diagnostic posture feeding the Reviser a located concern (#6); beyond that it touches no other architectural principle.

See the **BRAINSTORM** section of `workflows/world-forge-discovery.md`.

### 12. NPC Memory Manifest (producer contract with the npc-memory extension)

World Forge is the **producer** side of an external **NPC Memory Contract** (`schema: 1`) shared with the `npc-memory` SillyTavern extension (which lives in the ST fork, not this repo). The extension attaches per-NPC memory keyed by a **stable id** instead of a volatile UID. World Forge's only obligation is to emit a machine-readable index — the **manifest** — as an inert World Info entry embedded in the lorebooks the Compiler already builds.

- **Carrier.** A normal World Info entry whose `comment` begins with the literal token `[[NPC_MANIFEST]]` and that has `disable: true`, `key: []`. `disable: true` keeps it out of every prompt while leaving it in the loaded world-info data, where the extension reads it. At most one per lorebook. Its `content` is a single JSON object: `schema`, optional `lorebook`/`personas`/`scenes`, and `npcs[]` (each with a stable slug `id`, `displayName`, `aliases`, a `facets` map of reserved durable keys → source entry uid, and `relationships[]`).
- **Stable ids are the load-bearing element.** Memory is keyed by the slug `id` (lowercase, non-alphanumerics → `_`, collapsed, trimmed — `Anna Larsson` → `anna_larsson`), derived deterministically from the canonical name and **never from the UID** (UIDs renumber on re-export; ids must not). The protagonist (`{{user}}`) is a `persona`, not an npc.
- **Identity discipline is enforced, not assumed.** The whole contract rests on naming conventions, so they are pinned at the chokepoints rather than left to chance: the Architect's **NPC/Character Identity Convention** (§7) mandates one canonical name per character used verbatim in every comment, unique across the cast, em-dash form, a controlled facet vocabulary, and one character per entry (interchangeable extras marked `Shared roster entry` = one shared id). The Editor's **Step 4.6** hard-fails deviations (bad comment form, name inconsistency, slug collision, unmarked multi-person entry, `{{user}}` as an npc). The Compiler **halts** rather than emit a colliding manifest, and `tools/validate_export.py` re-checks slug validity + uniqueness deterministically. **Renames are the one unfixed gap** (a changed name re-slugs and orphans memory; ids are not persisted across renames — contract §4); the revise mini-Compiler flags any rename loudly for confirmation instead of shipping it silently.
- **Purely additive, never mandatory.** The manifest changes no existing entry and emits no new file. A world without it still works — the extension degrades to prose parsing. (Note: two comment conventions exist — aggregated NPC lorebooks use `NPC — Name (Facet)`, per-character Tier 2 lorebooks may use `Name — Aspect`. The extension's `^NPC —` prose fallback catches the former but not the latter, so the manifest is what makes every character discoverable. The Compiler normalizes both to one npc per character — Step 7.7d.)
- **The Compiler is the only writer** (principle #3 is unchanged — manifest emission is part of normal Export JSON authoring). Each manifest is a **per-file** carrier whose `facets`/`scenes` uids resolve within its own lorebook. The revise pipeline's mini-Compiler regenerates a touched lorebook's manifest against preserved + new UIDs, keeping slug ids stable. `tools/validate_export.py` is the deterministic backstop (carrier flags, JSON-parse, slug validity, uid resolution).
- **Boundaries.** The manifest does not touch SillyTavern itself, the override architecture (#2), audit/apply separation (#3), Position Rationale (#4), or the preset. The contract also defines a per-message "turn tag" runtime lever (`<!--npcmem:{…}-->`; fields `v`/`actors`/`present`/`withUser`/`scene`/`location`/`src`, with `src ∈ model|card|inferred`). As of `MEMORY_CONTRACT.md` §7 the turn tag is **consumer-owned**: the npc-memory extension injects the emit-instruction (authoritative ids + scene candidates), the model supplies the per-turn judgment, and the extension parses/strips/stores it and synthesizes an `inferred` fallback when absent. Producer-baked emission is explicitly *optional redundancy*, so World Forge has **no turn-tag obligation** — it stays a possible future preset/card-level enhancement, never a requirement. (Verified against the contract: World Forge's manifest payload — carrier flags, `lorebook.kind` enum, `personas.user`, npc `id/displayName/aliases/facets/relationships{to,kind,note}/tags`, scene `{id,uid,arc,seq,title}`, the slug rule, and scene-id form — matches schema 1 field-for-field; no producer change warranted.)

See **Step 7.7** of `agent_roles/04_The_Compiler.md` for the full operation.

### 13. The Posture Contract (`{{user}}` is a character, not the customer)

The model was optimized to satisfy the person typing, and that disposition does not switch off inside a fiction: it reads `{{user}}`'s message as *a request from someone it should please* rather than as *a character's move in a story*. Four failure modes follow, all observable in ordinary play — **yield** (an NPC concedes because `{{user}}` asked, not because its own interests say yes), **auto-success** (a stated attempt, "`{{user}}` reaches for the ledger", is rendered as an accomplished fact), **rescue** (a convenient interruption or lucky timing dissolves an outcome the scene had earned), and **rescue from consequence** (after `{{user}}` does something that should cost them, the cost is softened, walked back, or reported as having turned out fine).

The purpose is not harshness. **The emotional payoff is what fiction is for**, and a model protecting the player from that payoff deletes the thing they came for. This cuts both ways: it is what lets a souls-like field opposition that genuinely tries to beat `{{user}}`, and equally what keeps a gentle world from flattening into wish-fulfilment.

The jailbreak's `{{user}}`-as-actor clause (#2, block library §5a-detail clause 2) grants that harm *may* reach `{{user}}`. It does not touch the disposition — which is how a world ends up with the permission granted and never exercised. The posture contract is the correction, and like the embodied-specificity seam it is a **substrate + prohibition + audit triad**; none of the three works alone.

- **Substrate (world-specific).** World Seed §3 `Posture Toward {{user}}` → Master Design **Section 6**: a declared `Default posture`, a **force that does not defer** and what it wants instead, concrete **losable** things, the permitted shapes of a lost scene, the **boundary**, and — where the cast works *through* `{{user}}` — the manipulation vectors and tell rule. **Unstated is not neutral — it is deferential**, the same trap as the intimacy valence trichotomy (an absent field yields to a trained default, never to nothing). The non-deferring force is required in all five postures, because universal deference has nothing to measure itself against.
  - **The enum is five values, and it measures intent, not difficulty:** `adversarial` | `indifferent` | `mixed` | `deferential` | `predatory`. **Compliance and intent are independent** — `deferential` and `predatory` both yield to `{{user}}` constantly and are opposite worlds, because one has their interests at heart and the other is feeding on them. A compulsion-premise or manipulation world will be mis-declared as `deferential` almost every time unless the Interviewer asks whether the bending is *for* `{{user}}` or *at* them; the Refiner re-checks this against the seed's cast.
  - **Harm has five classes** — material, relational, physical, **moral** (complicity, self-image, a line they can't uncross), **epistemic** (deceived and acting on it). The model reaches only for the first three unprompted, and the last two are the *only* stakes available when `{{user}}` is invulnerable, omnipotent, or universally obeyed. A world like that answers "what can he lose" with "nothing" unless the classes are named.
  - **The boundary runs both directions.** Some worlds rule things out (no death, no maiming); souls-like and survival worlds must rule things explicitly **in**, because the model reinstates its own floor — it will write a desperate fight and then decline to let it be lost.
- **Binding directive (Tier 3).** The **stance-toward-`{{user}}` directive** is a mandatory ARC_STATE / SANDBOX_STATE Tonal Mandate category (Architect §8.A / §8S.A) — **every arc, no exemptions**, `deferential` worlds inverted rather than omitted and `predatory` worlds shaped as *use* rather than opposition (a `predatory` bullet written as NPCs refusing contradicts its own premise). It names *this arc's* opposition, what is live to lose, and the reflexes as prohibitions **in concrete vocabulary** (an abstract restatement of the generic rule is an Editor hard fail — the generic version is the preset's job). Where a tell rule is declared it is carried here as a concrete prose-habit prohibition, and manipulation vectors carry with their **voiced phrasings verbatim** — the wording is the mechanic and a paraphrase loses it. It is a directive to the world and its cast; a clause specifying what `{{user}}` does *or feels* in response is a hard fail.
- **Engine (preset, world-agnostic).** Jailbreak **clause 2b, role separation** — the human *authors* `{{user}}`'s moves rather than being served; their messages are a character's actions, not requests addressed to the assistant; the obligation runs to the story's integrity. Framed as role assignment, not prohibition, because "do not try to please the user" reads as an instruction to weigh against others while a role reframe changes what the model thinks it is doing. Plus two optional blocks, each carrying a **counter-guardrail that is not optional content**: **`protagonist_jeopardy`** (the four reflexes; guardrail = no agency seizure, no narrating `{{user}}`'s defeat, no no-win scenes, no sourceless adversity — *opposition, not punishment*), and **`antagonist_integrity`**, the cast-side sibling (no mitigation the drafts never authored, no narrator annotating a character's sincerity; guardrail = antagonists operate from a coherent frame and authored warmth is rendered in full — *no flat villains*). A block shipped without its guardrail produces a worse bug than its absence; Pass 2 hard-fails both.
- **Audit.** Voice Auditor **bid scenario class** (mandatory every arc, both modes, *shaped to the posture* — a demand against resistance in `adversarial`/`indifferent`/`mixed`, an NPC's ask *of* `{{user}}` in `deferential`/`predatory`) + **Step 3K**, testing the reflexes *and* the over-correction, judged by the counterfactual rather than by how smooth the scene read — a deferential scene reads as cooperative, well-written prose, which is why an unremarkable pass here is the default outcome and usually wrong. **Step 3L** is the cast-side counterpart (antagonist fidelity, via a cruelty scenario). Findings split by **breadth**: isolated = world-side (Architect, the stance directive or the card); uniform across the matrix = engine-side (Prompt Engineer, the preset blocks or clause 2b).

**Boundaries.** The posture block is **world-and-cast property, never protagonist property** — it lands in Tier 3 and never in the Tier 2 Protagonist Lorebook, where it would read as an instruction about the player's character (Editor Step 5.7d). It touches no other architectural principle. A posture change is an ordinary revision (`tier3_arc_tonal_recalibration` / `sandbox_state_recalibration`, Section 6 is not the Section 1 / 11a bright line) whose cascade spans **every** arc, not one. In a reframe conversion the block always regenerates (every field is computed against a specific protagonist); in rebaseline it keeps.

**Adjacent and not yet built:** world-integrity enforcement against `{{user}}`'s *inputs* — a declaration impossible in this world (a firearm in a medieval setting, a resource the character does not have). That is the same root cause pointing the other way (the fiction outranks momentary comfort) but a different check: the model should decline the *premise* rather than resolve the attempt, which the auto-success prohibition does not reach. Tracked as follow-up; clause 2b is its foundation.

### 14. `{{user}}` is reference data, never impersonation guidance (both protagonist artifacts)

The human plays `{{user}}` — the pipeline never instructs the model on how to *be* them. The rule was stated upstream in three places (Architect §5.5, Refiner Section 6, `templates/User_Persona_template.md`) but enforced on **`Drafts/User.md` alone** (Editor Step 5.5c), leaving the Tier 2 Protagonist Lorebook ungated — the unbounded file, holding the content nearest the line, that Step 5.5d actively routes `User.md` overflow *into*. Both artifacts are now gated: 5.5c for the persona description, **Step 5.7** for the lorebook.

**The discriminator is grammatical mood and subject, not topic.** Every Section 3 topic — psychology, hidden layer, powers, history, body, relationship stances, `{{user}}` intimate embodiment (the Intimacy Architect's §6.6 profile) — is legitimate lorebook content. What fails is a sentence whose acting subject is `{{user}}`: *"Andrei goes still under pressure and lets the silence work"* (behavioral mandate) versus *"Andrei is physically still under pressure; people read it as composure or contempt, and both readings are common"* (perception fact). The second is strictly more useful, because it names the reaction the model must actually produce.

Two rules keep the gate from doing damage of its own: **every hard fail ships with a reframed alternative** (Editor 5.7b — a bare rejection reliably produces deletion of load-bearing material rather than a reframe, and a second-round deletion of a fact you asked to be reframed is itself a flag), and the diagnostic list is **relaxed relative to 5.5c** for this file (`always`/`never`/`voice`/`tone` soft-flag rather than hard-fail; they appear constantly in legitimate world-facing reference). Do not over-correct into stripping the protagonist lorebook of depth — that is the opposite failure and it costs the model the reference data it needs.

---

## Cross-file consistency requirements

These pairs of files must stay in sync. When editing one, check the other.

| If you edit | Also check |
|---|---|
| `agent_roles/02_The_Architect.md` (entry templates) | `agent_roles/03_The_Editor.md` (validation rules) — they validate what the Architect produces |
| `agent_roles/02_The_Architect.md` (card structure) | `templates/Char_Card_creation.md` — both define card field requirements |
| `agent_roles/02_The_Architect.md` / `agent_roles/03_The_Editor.md` (User.md persona) | `templates/User_Persona_template.md` — both define the `{{user}}` persona-description structure |
| `agent_roles/05a_Block_Library.md` (block library, split from 05) | `agent_roles/05_The_Prompt_Engineer.md` (Sections 5b–5f reference §5a/§5a-detail; Resync Mode re-derives from it; the Section 5.0b **genre-lens hint table** enumerates the §5a optional menu by genre family — adding/renaming an optional block updates that table in the same edit) and `templates/Chat_Completion_Preset_template.json` — agent must produce against template structure |
| Director-card style handling (principle #2/#9 seam): `agent_roles/SHARED_Style_Contract_Reference.md` §3a-D Director-variant `NARRATIVE PERSPECTIVE` templates + §3d `DIRECTOR-CARD RULE` verbatim line | `agent_roles/01_The_Refiner.md` (Step 1.5 Pass 4 Director scan runs regardless of world default; Section 11c `has_director_card` + Director-flagged list; Section 11d advisory covers all focal-anchored effective perspectives), `agent_roles/02_The_Architect.md` (§3a-D selection for Director-flagged overrides + worked example), `agent_roles/03_The_Editor.md` (Step 5.6 Pass 2 hard-fails §3a prose on a Director card and §3a-D prose on a non-Director card), `agent_roles/05_The_Prompt_Engineer.md` + `agent_roles/05a_Block_Library.md` (conditional DIRECTOR-CARD RULE line in the world `<style_contract>`, Pass 1 presence/absence hard checks, resync adds a missing-but-warranted line), `agent_roles/revise/05_The_Prompt_Engineer_mini.md` (Trigger C `has_director_card` flip) — the Director criteria, the variant prose, and the verbatim rule line must read identically across all of them. The rule exists because `{{char}}` resolves to the Director card's *name* at runtime; it is the stock-ST-safe correction for "focal on [Director name]" |
| `Notes_On_functionality.md` (any correction or content change) | `Notes_Quick_Reference.md` — it is a DERIVED distillation (position enum, flags, assembly order, gotchas); regenerate the affected fact, never let the two disagree |
| `agent_roles/04_The_Compiler.md` (pre-save guards) | `tools/validate_export.py` — the validator deterministically re-checks the same failure modes (UTF-8/mojibake, parse, `{{original}}`, position enum, UID uniqueness, entry-key/UID parity, snake_case alias fields, leaked inline revision markers); changing a guard may require updating the script and vice versa |
| NPC Memory Manifest (principle #12): `agent_roles/04_The_Compiler.md` Step 7.7 | `agent_roles/revise/04_The_Compiler_mini.md` (regenerates a touched lorebook's manifest against preserved/new UIDs; rename flag) and `tools/validate_export.py` (`check_manifest`: carrier flags, JSON parse, slug validity + uniqueness, facet/scene uid resolution) — the carrier format, payload shape, and slug rule must read identically across all three; the external contract lives in the ST fork (`npc-memory` extension), so changing the payload shape is a cross-repo change |
| NPC identity discipline the manifest depends on (principle #12): `agent_roles/02_The_Architect.md` §7 **NPC/Character Identity Convention** (canonical name, em-dash comment form, facet vocabulary, one-character-per-entry / `Shared roster entry`) | `agent_roles/03_The_Editor.md` Step 4.6 (hard-fails comment form, name inconsistency, slug collision, multi-person entries, `{{user}}`-as-npc) and `agent_roles/04_The_Compiler.md` Step 7.7d–7.7j (derives ids/facets/aliases against it, halts on collision) and `tools/validate_export.py` (slug validity + uniqueness) — the canonical-name rule, comment form, and facet labels must read identically across the authoring spec, the validation gate, the emitter, and the backstop |
| Any agent spec's INPUT section | The same spec's `📂 CONTEXT MANIFEST` block at the top — the manifest is the load-discipline restatement of the inputs; they must list the same files |
| `agent_roles/05_The_Prompt_Engineer.md` Section 8 (Preset Resync Mode) | `workflows/world-forge-postlaunch.md` PRESET RESYNC section — both define the `/worldforge resync-preset` operation |
| `Notes_On_functionality.md` (position table) | All agent files referencing positions — they cite this table |
| Any agent spec (sign-off block) | The phase's stage file (`workflows/world-forge-discovery.md` / `-drafting.md` / `-validation.md` / `-construction.md` phase descriptions) and the router's DISPATCH PROTOCOL artifact table — phase outputs feed handoffs, and each sign-off heading is that phase's artifact-gate anchor string |
| Router/stage dispatch contract: `workflows/world-forge.md` (ROUTER & STAGE FILES map, DISPATCH PROTOCOL artifact-gate table, TRIGGER COMMANDS stage pointers, PIPELINE STATE LEDGER anchor column) | The four stage files (entry/exit conditions + per-phase artifact gates), every agent's `## ✅ … SIGN-OFF` heading (the anchor strings the gates grep for), `tools/validate_pipeline_state.py` (re-checks the same phase → artifact → anchor mapping deterministically), and `AGENTS.md` (session routing + the dispatch-never-inline rule) — stage boundaries, report filenames, and anchor strings must read identically across all of them; renaming a sign-off heading or a report filename breaks the artifact gate and the validator together |
| `agent_roles/06_The_Intimacy_Architect.md` | `agent_roles/03d_The_Intimacy_Auditor.md` — auditor validates what architect produces |
| Embodied specificity (the generic-body defense): `agent_roles/06_The_Intimacy_Architect.md` Entry 3 **Half A embodied baseline** (age + body history, build/scale, arousal/recovery mechanics, this world's act particulars, trajectory) and Entry 7 **Half B physical dyad** (height/build/stamina/age-gap/experience differentials *and their stated consequence*, per pairing) | `agent_roles/03d_The_Intimacy_Auditor.md` (Step 3I body specificity / dyad materiality / clinical intrusion / deficit framing + diagnosis rows), `agent_roles/00_The_Interviewer.md` (Section 4 substrate elicitation), `agent_roles/01_The_Refiner.md` (Section 7 recording + sign-off), `templates/World_Seed_Template.md` (§4 `Embodied baseline` + `Physical dyad` fields, NPC intimacy fields, §4 checklist), `agent_roles/06_The_Intimacy_Architect.md` §6.5 (roster `Embodied baseline` line), `agent_roles/revise/02b_The_Intimacy_Architect_mini.md` (delta 3a + stale-dyad cross-cascade), `agent_roles/revise/03d_The_Intimacy_Auditor_mini.md` (delta 3c), and the Converter three-file contract (`agent_roles/Converter/00_The_Converter.md` matrix rows + Section 9 rebaseline inversion, `templates/Convert_Brief_Template.md` §4e) — **three rules must read identically everywhere: observable register not clinical vocabulary** (the entry reaches the model's context and the model echoes it), **difference not deficit** (both directions, both partners), and **every intimate character gets a baseline including the default-bodied ones** (otherwise it becomes "the aging field" and the stock default silently wins elsewhere). The dyad is a *pairing* property no per-character field can hold — which is why a protagonist swap strips `{{user}}` dyads while embodied baselines carry |
| The reflex half of the same seam (facts alone do not displace trained defaults): `agent_roles/06_The_Intimacy_Architect.md` **§6.6 `{{user}}` intimate embodiment** (Tier 2 protagonist intimacy profile — reference data for other characters' reactions, **never** an instruction to play `{{user}}`) and **§6.7 stock-register prohibition** (scale language bound to the authored bodies, act cost derived not defaulted, no character on a default body — carried in `INTIMATE_HARD_RULES`, authored from §8a defaults when the seed omits them) | `templates/World_Seed_Template.md` (§3 `Protagonist Intimate Embodiment`, §8a recommended stock-register rules, §4 anatomical-fit + valence clauses), `agent_roles/00_The_Interviewer.md` (Section 3 elicitation + framing preamble; Section 8 proposes the stock-register rule by default), `agent_roles/01_The_Refiner.md` (Section 6 recording + gap routing), `agent_roles/03d_The_Intimacy_Auditor.md` (Step 3I stock-register override / `{{user}}` embodiment / valence integrity sub-checks + the Step 1 **embodiment probe** scenario class + Critical severities), `agent_roles/03_The_Editor.md` (protagonist intimacy profile in the per-entry file list), `workflows/world-forge-drafting.md` (Phase 2.5 outputs), `agent_roles/revise/02b_The_Intimacy_Architect_mini.md` (delta 3a-i + `{{user}}`-body-change cross-cascade), `agent_roles/revise/03d_The_Intimacy_Auditor_mini.md` (delta 3c), Converter three-file contract (matrix rows: protagonist embodiment regenerates with Section 3 / kept in rebaseline; stock-register rules preserve) — the **bright line** (`{{user}}` embodiment is reacted-to reference data, never a protagonist behavioral mandate), the three prohibitions, and the **valence trichotomy** (neutral fact / advantage / charged; unstated = coin flip between two opposed defaults, never neutral) must read identically across all of them |
| Intimate aftermath (the fourth trained reflex — scenes terminating at climax): `agent_roles/06_The_Intimacy_Architect.md` **Entry 6b `[CHAR]_AFTERMATH`** (ordinary bodily business + what they do with the other person + the change-of-state tell) and the §6.7 aftermath prohibition | `templates/World_Seed_Template.md` (§4 `Afterward` field + both NPC intimacy fields + §8a recommended rule + checklist), `agent_roles/00_The_Interviewer.md` (Section 4 aftermath question), `agent_roles/01_The_Refiner.md` (Section 7 recording), `agent_roles/03d_The_Intimacy_Auditor.md` (Step 3A aftermath fidelity, Step 3I termination reflex, **Step 2's past-climax scenario requirement**, two diagnosis rows), `agent_roles/06_The_Intimacy_Architect.md` §6.5 (roster `Afterward` line), `agent_roles/revise/02b_The_Intimacy_Architect_mini.md` + `agent_roles/revise/03d_The_Intimacy_Auditor_mini.md` — the substrate and the prohibition are a **pair**: the entry alone is authored and never reached (the model stops writing first), and the prohibition alone has nothing character-specific to reach for. The Auditor's past-climax scenario requirement is what keeps the check exercisable — a matrix whose scenes all end at the act passes Step 3I while the substrate is never read |
| The posture contract (principle #13 — the substrate half): `templates/World_Seed_Template.md` §3 `POSTURE TOWARD {{user}}` (declared posture enum, non-deferring force, losables, failure shapes + off-the-table boundary) | `agent_roles/00_The_Interviewer.md` (Section 3 elicitation with its framing preamble + sign-off line), `agent_roles/01_The_Refiner.md` (Master Design Section 6 `Posture Toward {{user}}` record block, Tier 3 routing statement, gap routing, sandbox charter cross-check, sign-off), `agent_roles/02_The_Architect.md` (§8.A **STANCE-TOWARD-{{user}} DIRECTIVE** block + §8S.A sandbox counterpart + both Tier 3 checklists), `agent_roles/03_The_Editor.md` (Step 4a-3d unconditional hard fail + the 4a-S sandbox line + sign-off), `agent_roles/03b_The_Voice_Auditor.md` (bid scenario class + Step 3K + diagnosis rows + report section + sign-off), `workflows/world-forge-discovery.md` + `workflows/world-forge-validation.md` (Phase 0/1/3.5 descriptions) — **five rules must read identically everywhere: unstated posture is deference, not neutrality** (same shape as the intimacy valence trichotomy); **the enum is five values and measures intent, not difficulty** — `deferential` and `predatory` both comply and are opposite worlds, so every consumer must state that compliance ≠ intent; **the non-deferring force is required in all five postures** (in `predatory`, as "what does not defer though the people do"); **harm has five classes** and `moral`/`epistemic` must be named explicitly, since they are the only stakes an untouchable protagonist has; and **the stance directive is mandatory in every arc with no exemptions**, inverted in `deferential` worlds and shaped as *use* rather than opposition in `predatory` ones. The field names, the posture enum values, the harm-class names, and the tell-rule values (`opaque` / `visible-but-tempting` / `mixed by character`) are a pinned vocabulary — the Refiner's record block, the Architect's directive, and the Editor's traceability check all key off them. Manipulation vectors carry their **voiced phrasings verbatim** through every hop; a paraphrase loses the mechanic |
| The tell rule + antagonist fidelity (principle #13, cast side): `agent_roles/02_The_Architect.md` §8.A **THE TELL RULE** block + §5 **ANTAGONIST FIDELITY** block | `agent_roles/03_The_Editor.md` (Step 4a-3d tell-rule hard fail — concrete prose-habit prohibitions, not an abstraction; Step 3 antagonist-interiority soft flag), `agent_roles/03b_The_Voice_Auditor.md` (Step 3K tell-rule integrity; **Step 3L** antagonist fidelity + cruelty scenario class + diagnosis rows + report section + sign-off), `agent_roles/05a_Block_Library.md` (**Antagonist Integrity** menu entry + §5a-detail), `agent_roles/05_The_Prompt_Engineer.md` (antagonist-integrity block hint, Pass 2 checks, sign-off) — **three rules must read identically: the narrator does not annotate sincerity** (behavior and dialogue carry it); **"feels nothing" and "loves them and does it anyway" are specifications, not gaps** — the two configurations the model will not hold unaided; and the **counter-guardrail against flat villainy** is non-optional content, exactly as the jeopardy block's is. The tell-rule enum values are pinned |
| The posture contract (principle #13 — the engine half): `agent_roles/05a_Block_Library.md` jailbreak **clause 2b** (role separation) + the **Protagonist Jeopardy** and **Antagonist Integrity** menu entries and their §5a-detail content requirements | `agent_roles/05_The_Prompt_Engineer.md` (5.0b protagonist-jeopardy and antagonist-integrity block hints, Pass 1 five-clause jailbreak check, Pass 2 block-content + inclusion checks, sign-off lines, Section 8 resync ADD + the clause-2b drift note) and `agent_roles/revise/05_The_Prompt_Engineer_mini.md` (Trigger G block toggle; clause 2b explicitly out of revise scope → resync) — each block's **counter-guardrail** must be stated as non-optional content in every one of them (jeopardy: no agency seizure, no narrating `{{user}}`'s defeat, no no-win scenes, no sourceless adversity — *opposition, not punishment*; integrity: coherent frame, authored warmth rendered in full — *no flat villains*). A block without its guardrail contradicts another engine rule and is a Pass 2 hard fail, not a soft flag. Two counts are load-bearing prose: the jailbreak is described as **five clauses** and the jeopardy block as **four failure modes** in the library, in the hints, and in Pass 1/2 — changing one site without the others leaves a validator checking the wrong number |
| The posture contract's revise + convert cascades (principle #13) | `agent_roles/revise/00_The_Reviser.md` (`tier3_arc_tonal_recalibration` / `sandbox_state_recalibration` rows carry the posture-change case **and the every-arc cascade warning**), `agent_roles/revise/02_The_Architect_mini.md` (R2.5 stance-bullet survival + multi-arc scope note), `agent_roles/revise/03_The_Editor_mini.md` (R3.1 stance-bullet regression check + the all-arcs-or-none check), `agent_roles/revise/03b_The_Voice_Auditor_mini.md` (delta 9 — bid scenario on state/posture scopes, with the over-correction weighted), and the Converter three-file contract (`agent_roles/Converter/00_The_Converter.md` matrix row `Section 3 — Posture Toward {{user}}` = regenerate-always + Step 4 question 7 + Step 6 Section 3 authoring + Section 9 rebaseline keep-row, `templates/Convert_Brief_Template.md` §5) — a posture revision cascades to **every** arc (a half-updated world plays as neither posture), and a reframe **always** regenerates the block because every field is computed against a specific protagonist |
| `{{user}}`-as-reference-data (principle #14): `agent_roles/02_The_Architect.md` §7 **THE TIER 2 PROTAGONIST LOREBOOK** block (descriptive-for-reaction vs. directive-for-behavior table, the reframing rule, the not-forbidden list) | `agent_roles/03_The_Editor.md` **Step 5.7** (5.7a directive scan with its two deliberate relaxations from 5.5c, 5.7b mandatory reframe suggestion, 5.7c ambiguous-mood soft flag, 5.7d posture-misrouting fail) and Step 5.5c (`User.md`, the sibling gate) and `agent_roles/01_The_Refiner.md` Section 6 note and `templates/User_Persona_template.md` and `agent_roles/revise/03_The_Editor_mini.md` (R3.1 protagonist-lorebook gate on touched entries) and `agent_roles/02_The_Architect.md` pre-submission checklist — **the discriminator is grammatical mood and subject, not topic**, and **every hard fail must ship with a reframed alternative** (a bare rejection produces deletion of load-bearing material, not a reframe). The relaxations are deliberate: this file's legitimate content differs from a 150-word identity floor, so `always`/`never`/`voice`/`tone` soft-flag here and hard-fail there |
| `agent_roles/03b_The_Voice_Auditor.md` (Step 3 check taxonomy: register match, trigger-response, reflex misfire, NPC goal-trace, protagonist jeopardy, "would the model invent this") and `agent_roles/03d_The_Intimacy_Auditor.md` (intimacy lens) | `agent_roles/Auditioner/00_The_Auditioner.md` — the on-demand probe **reuses these checks by reference** rather than duplicating them (Section 4 Step 3); if a check's name or semantics change, update the Auditioner's citation. Also `workflows/world-forge-postlaunch.md` AUDITION section + `.kilo/kilo.jsonc` `WorldForge-Auditioner` seat |
| Any parent agent in `agent_roles/*.md` | Its mini counterpart in `agent_roles/revise/*_mini.md` — the mini inherits parent's foundational rules, so changing the parent may require updating the mini's delta list |
| Revision replace-not-stack contract (principle #6 "Inline revision markers"): `agent_roles/revise/01_The_Refiner_mini.md` (R1.5 in-place merge), `agent_roles/revise/02_The_Architect_mini.md` (Foundational Rule 8 + R2.5/R2.6), `agent_roles/revise/02b_The_Intimacy_Architect_mini.md` (R2.5.x) | `agent_roles/revise/03_The_Editor_mini.md` (Step R3.1b no-stacking hard-fail gate) and `agent_roles/revise/04_The_Compiler_mini.md` (Foundational Rule 9 marker strip) and `tools/validate_export.py` (leaked-marker backstop) — the producers must replace in situ, the Editor must catch any residual stack, the Compiler strips the annotation; the "replace, don't stack / one current version per passage" rule must read identically across all of them |
| `workflows/world-forge-revise.md` (routing matrix) | `agent_roles/revise/00_The_Reviser.md` (scope types) — both must enumerate the same fourteen scope types (incl. the three `sandbox_*` types) |
| `agent_roles/02_The_Architect.md` (sandbox lorebook + Roster NPC §7.E) | `agent_roles/03_The_Editor.md` (sandbox validation) and `agent_roles/03b_The_Voice_Auditor.md` (distinctiveness lens) — Editor validates `SANDBOX_STATE`/`WORLD_PULSE`/roster format; Voice Auditor tests roster fingerprint uniqueness |
| NPC agency: Architect §7.D principal **Standing Goal** (+ optional **Escalation Ladder** — stages/conditions/endpoint/collision in Tier 2, active stage in Tier 3 via the NPC_SHIFT stage line / SANDBOX_STATE-WORLD_PULSE naming) + ARC_STATE/SANDBOX_STATE **activity cadence** directive | `agent_roles/03_The_Editor.md` (Step 4a-3b cadence + goal-coverage hard checks; Step 4a-3c ladder integrity + stage-named cadence), `agent_roles/03b_The_Voice_Auditor.md` (Step 3J initiative + goal-trace; stage-trace + stage-discipline probe for laddered NPCs), `agent_roles/03c_The_Arc_Transition_Auditor.md` (Check 3b ladder-stage continuity — monotonic, condition-satisfying, no off-page endpoint), `agent_roles/00_The_Interviewer.md` + `01_The_Refiner.md` (elicit/record the goal and the ladder), `templates/World_Seed_Template.md` (NPC Standing Goal + Escalation Ladder fields), `agent_roles/05a_Block_Library.md` (`npc_ensemble` guardrail's ladder-execution clarification), `agent_roles/05_The_Prompt_Engineer.md` (Section 5.0b ladder-aware `npc_ensemble` selection hint for arc worlds + checklist line) — the goal/ladder is authored once and pointed at by the cadence directive; all must stay in sync across arc and sandbox |
| Relationship/belief state: Architect CHARACTER_STATE item 6 + NPC_SHIFT relational-stance delta (arc); standing accumulation via SANDBOX_STATE aliveness (sandbox) | `agent_roles/03c_The_Arc_Transition_Auditor.md` (Check 3b — continuity teeth: no teleporting bonds, un-caused belief flips, or silent memory resets), `agent_roles/03_The_Editor.md` (relational-stance delta-integrity + coverage soft-flag), `agent_roles/00_The_Interviewer.md` + `01_The_Refiner.md` (elicit/record the drift trajectory + operative belief), `templates/World_Seed_Template.md` (§4 "How it drifts"/"Operative belief" + arc "Relationship shifts this arc") — drift is authored per-arc as delta only, and Check 3b verifies it is earned; arc-mode only (sandbox has no seam) |
| Trauma de-escalation: Architect CHARACTER_STATE item 7 (trauma trajectory delta, arc) | `agent_roles/03c_The_Arc_Transition_Auditor.md` (Check 2 trauma-response continuity — fades shown, never sudden vanishings/un-caused re-activations), `agent_roles/03b_The_Voice_Auditor.md` (Step 3A active-vs-dormant match against item 7), `agent_roles/03_The_Editor.md` (trauma-trajectory delta-integrity + coverage soft-flag), `agent_roles/00_The_Interviewer.md` + `01_The_Refiner.md` (elicit/record which trigger fades in which arc), `templates/World_Seed_Template.md` (§4 "Trauma trajectory" + arc "Trauma shifts this arc") — authored per-arc as delta; intimate-context trauma rides the per-arc Intimacy Register instead; arc-mode only (sandbox trauma is static Tier 2 substrate) |
| World Mode handling in any one of Interviewer / Refiner / Architect / Editor / Voice Auditor / Intimacy Architect / Intimacy Auditor / Compiler / Prompt Engineer | The others + `workflows/world-forge.md` WORLD MODE (ARC vs. SANDBOX) section + `templates/World_Seed_Template.md` (§1 `World Mode`, §5 conditional, §4 NPC intimacy) — the arc/sandbox branch (incl. the standing Sandbox Intimacy Register and NPC principal/roster intimacy) must read consistently end-to-end |
| `agent_roles/Converter/00_The_Converter.md` (preservation matrix, Conversion Manifest format, Section 9 Rebaseline mode) | `templates/Convert_Brief_Template.md` (Sections 4a–4h mirror the matrix row-for-row; *Rebaseline:* notes mirror the Section 9 inversions) + `workflows/world-forge-convert.md` (operating modes incl. REBASELINE MODE, overlap floor / zero-axes gate, role reassignment cases) + `templates/World_Seed_Template.md` (the seed structure the Converter writes against — section numbering, required fields, conventions) — Convert is a three-file contract; changing any one requires checking the others |
| NPC agency / relationship-belief state / trauma-trajectory machinery (Architect §7.D `Standing Goal` + `Escalation Ladder`, World Seed §4 `How it drifts` + `Operative belief` + `Trauma trajectory`, Refiner/Architect/Editor/Voice-Auditor/Arc-Transition-Auditor rules) | `agent_roles/Converter/00_The_Converter.md` (Section 4 carry-across rules + preservation matrix) and `templates/Convert_Brief_Template.md` (Section 4e reminders) — these Section 4 fields couple to the regenerated parts of a converted seed (Section 3 protagonist + Section 5 arcs). The Converter's carry-across rules (preserve / strip / mark-for-reauthor; the ladder rides the Standing Goal rule with per-stage and collision audits, active stage never carries) must stay consistent with how the parents author the fields; if the field set changes, update the Converter's Section 4 handling at the same time |
| `agent_roles/00_The_Interviewer.md` (Section 3 protagonist questioning; Section 9 seed-revision posture) | `agent_roles/Converter/00_The_Converter.md` (Step 4 question 7 reuses the Interviewer's Section 3 depth standard; Section 9 Step H dispatches the Interviewer's seed-revision posture for `--then-interview` / `--then-brainstorm`) — Convert authors a new protagonist with Interviewer-grade depth and chains rebaseline into the posture; if either side's contract evolves, the other inherits |
| `agent_roles/Brainstormer/00_The_Brainstormer.md` (optional ideation, #11) | `agent_roles/00_The_Interviewer.md` (Context Manifest "Load if present" + Section 4 interview-open note read `Brainstorm_Notes.md` as starting material; Section 9 reads improvement-posture notes as *proposals*), `agent_roles/revise/00_The_Reviser.md` (`--brainstorm` invocation + INPUT reads revision-diagnostic `Brainstorm_Notes.md` as captured intent + Step 2 off-ramp), `agent_roles/Converter/00_The_Converter.md` (Section 9 Step H `--then-brainstorm` dispatches the Brainstormer's improvement posture ahead of the seed-revision Interviewer), `workflows/world-forge.md` (trigger rows) + `workflows/world-forge-discovery.md` (BRAINSTORM section) + `workflows/world-forge-convert.md` + `workflows/world-forge-revise.md` (`--then-brainstorm` chain, `revise --brainstorm` diagnostic front door), `.kilo/kilo.jsonc` (`WorldForge-Brainstormer` seat) — the Brainstormer's four postures (incl. the `--adapt` adaptation posture, which the Interviewer reads as a `fresh-start`-equivalent stamp, and the improvement posture's two doors: the `--then-brainstorm` chain writing run-scoped `Brainstorm_Notes.md` and the standalone no-commitment `brainstorm --improve` writing the standing `Big_Brain_Storm.md`), the `--then-brainstorm` chain, the `revise --brainstorm` diagnostic, and the `Big_Brain_Storm.md` pickup contract (revision-diagnostic step 1a and the chained improvement door's step 1a *offer* to adapt a parked idea, the chained door carrying the curated file forward to the target; the Converter's rebaseline Step F surfaces the file's existence only, never reading it; the Reviser and Interviewer never read the standing file itself) must read consistently across the spec, the Interviewer and Reviser that consume its notes, the Converter that invokes it, the orchestrators, and the Kilo config |
| World Calendar seam (`contracts/WORLD_FORGE_SYNC.md` §5 — the `[[WORLD_CALENDAR]]` Scene Tracker date seed): `templates/World_Seed_Template.md` (§2g calendar fields) | `agent_roles/00_The_Interviewer.md` (Section 2 optional elicitation) + `agent_roles/01_The_Refiner.md` (Master Design Section 1 `World Calendar (Scene Tracker seed)` line, 0-indexed months / 0–6 weekday) + `agent_roles/02_The_Architect.md` (§6 `[[WORLD_CALENDAR]]` carrier block) + `agent_roles/03_The_Editor.md` (Step 4.7 carrier validation) + `agent_roles/04_The_Compiler.md` (Step 7.8 emission into the World Lorebook) + `tools/validate_export.py` (`check_world_calendar`, WARN-only) — the carrier is **enabled** (`disable: false`, the one flag that differs from the `[[NPC_MANIFEST]]` carrier so the Scene Tracker's `!disable` read sees it), inert (`key: []`, `constant: false`), months **0-indexed** (0 = January); the marker, the flag, and the indexing must read identically across the seed field, the four agents, the validator, and the canonical contract. Cascades: `agent_roles/revise/04_The_Compiler_mini.md` **preserves** an existing calendar carrier through a World-lorebook rewrite (never drops it, never adds one; editing the calendar is an ordinary `tier1_world_rule_modify` whose minis inherit the parent §6/Step 4.7), and the Converter three-file contract (`agent_roles/Converter/00_The_Converter.md` preservation matrix + Section 2 seed-writing, `templates/Convert_Brief_Template.md` §4d) carries it across a reframe when the era is unchanged / regenerates it when the setting's time period differs / keeps it by default in rebaseline. The external consumer is the `world-forge` extension in the ST fork, so changing the payload shape is a cross-repo change |
| Dice Oracle seam (`contracts/DICE_ORACLE.md` — the `[[DICE_TABLES]]` Scene Tracker roll-table seed, schema 2): `templates/World_Seed_Template.md` (§2h pools / outcome scales / branches / tense / duration) | `agent_roles/Brainstormer/00_The_Brainstormer.md` (Section 4 + Cast & Voice lens float the dice oracle as a **leaning-only** suggestion when the premise leans on recounted off-screen pasts / a conjured temp cast — across default / improvement / adaptation, and as a diagnostic hook in the revision-diagnostic posture; never authors — the Interviewer/Architect do) + `agent_roles/00_The_Interviewer.md` (Section 2 optional elicitation, incl. recount-vs-event tense + duration) + `agent_roles/01_The_Refiner.md` (Master Design Section 1 `Dice Oracle Tables (Scene Tracker seed)` line, carrying tense/duration) + `agent_roles/02_The_Architect.md` (§6 `[[DICE_TABLES]]` carrier block: `schema: 2`, pools/procedures/gated steps, optional per-procedure `mode` recount/event + `turns`) + `agent_roles/03_The_Editor.md` (Step 4.8 carrier validation) + `agent_roles/04_The_Compiler.md` (Step 7.9 emission into the World Lorebook) + `tools/validate_export.py` (`check_dice_tables`, WARN-only) — the carrier is **enabled** (`disable: false`, same convention as `[[WORLD_CALENDAR]]` so the Scene Tracker's `!disable` read sees it), inert (`key: []`, `constant: false`); `schema: 2`, the `[[DICE_TABLES]]` marker, the enabled flag, and the `mode`/`turns` fields must read identically across the seed field, the four agents, the validator, and the canonical contract. The **"roll the shape, not the choreography"** authoring guidance (one situation = one procedure with gated participant *trait* steps, never per-participant procedures/rolls or per-participant choreography/finishes; count > 1 states simultaneity in `text` + an encounter-level configuration + one joint outcome; framing fixes register not order) is likewise a set-piece: it must read consistently across `templates/World_Seed_Template.md` §2h (author callout), `agent_roles/00_The_Interviewer.md` (elicitation steer), `agent_roles/02_The_Architect.md` §6 ("Roll the shape, not the choreography"), `agent_roles/03_The_Editor.md` Step 4.8 (soft-flags), and `contracts/DICE_ORACLE.md` §6.1. **Runtime interpretation (the engine half of the what/how split) lives in a preset block:** the optional `dice_oracle` block — `agent_roles/05a_Block_Library.md` (menu entry + §5a-detail content requirements: skeleton-not-script, never recite, multi-participant = one continuous scene, honor tense, defer the *how* to the world; engine-level, no world specifics) selected by `agent_roles/05_The_Prompt_Engineer.md` (5.0b dice-oracle block hint: default-include iff the world declares a `[[DICE_TABLES]]` oracle; Pass 1 presence-iff-oracle + no-specifics check; sign-off checklist; resync ADD) and toggled on by `agent_roles/revise/05_The_Prompt_Engineer_mini.md` (Trigger F, when a revision adds the world's first dice oracle). It is the `<dice_oracle>`-channel sibling of Block 4 Lore Integration's anti-recitation rule and sits on the preset (engine) side of the override contract (#2) — never in cards. Cascades: editing/correcting the tables is an ordinary `tier1_world_rule_modify` — the **remediation path** for a shipped oracle that serializes or over-rolls choreography is documented across `agent_roles/revise/00_The_Reviser.md` (the `tier1_world_rule_modify` row names the dice-carrier target + possible paired `intimacy_register_modify`), `agent_roles/revise/02_The_Architect_mini.md` (Step R2.5 inert-carrier edit: rewrite the payload in place applying parent §6, incl. shape-not-choreography), and `agent_roles/revise/03_The_Editor_mini.md` (Step R3.1 revalidates per parent Step 4.8 + the shape-not-choreography soft-flags); `agent_roles/revise/04_The_Compiler_mini.md` **preserves** an existing dice carrier's UID through a World-lorebook rewrite (never drops it, never adds one). The Converter three-file contract (`agent_roles/Converter/00_The_Converter.md` preservation-matrix §2h row, `templates/Convert_Brief_Template.md` §4d) carries it across a reframe when the conjured-fact situations still apply / regenerates or drops it when the protagonist or setting change makes them irrelevant / keeps it by default in rebaseline. The external consumer is the `world-forge` extension in the ST fork, so changing the payload shape is a cross-repo change |
| Runtime Directives channel (World Seed §9 → Master Design §12 → preset blocks): `templates/World_Seed_Template.md` (§9 directive format: behavior + wrong-response example + scope; routing guardrails) | `agent_roles/00_The_Interviewer.md` (interview-structure SECTION 9 elicitation + reroute rules + sign-off line — not the spec's own Section 9, which is the seed-revision posture) + `agent_roles/01_The_Refiner.md` (Step 1.6 classification, Master Design Section 12 `RD-N` record format, sign-off) + `agent_roles/05_The_Prompt_Engineer.md` (5.0b Step 2b intake, Runtime Directive Coverage table, Pass 2 check, resync Step 2 re-derivation) + `agent_roles/05a_Block_Library.md` (directives as a custom-block source) + `workflows/world-forge-discovery.md` + `workflows/world-forge-construction.md` (Phase 0/1/5 descriptions) + the Converter three-file contract (`agent_roles/Converter/00_The_Converter.md` matrix row + Step 6 Section 9 authoring, `templates/Convert_Brief_Template.md` §4i) — the directive record shape (`behavior`/`wrong_response`/`scope`), the ~6-directive ceiling, and the **forbidden-target rule** (directives never land in Main, Jailbreak, Formatting, or inside `<style_contract>` — those are the world-agnostic engine surfaces of principle #2) must read identically across the seed field, the three agents, the block library, the orchestrator, and the Convert contract |
| Checkpoint discipline (`workflows/world-forge.md` CHECKPOINT DISCIPLINE section) | `agent_roles/00_The_Interviewer.md` (Section 3 checkpoint paragraph + Section 7 incremental-authoring note), `agent_roles/01_The_Refiner.md` (Step 3), `agent_roles/02_The_Architect.md` (Section 4 + pre-submission File Integrity block), `agent_roles/06_The_Intimacy_Architect.md` (Section 5), `agent_roles/04_The_Compiler.md` (write-verification & resume note after the FILE-WRITING & ENCODING guard) — the three rules (unit-grain commits, verify-landed, resume-from-disk) and the one-entry/one-section grain floor must read consistently across the canonical contract and every agent application; the Editor, auditors, and Prompt Engineer are covered generically by the workflow section (their unit is a single report file) and carry no per-spec text; revise minis inherit the parents' checkpoint rules with no delta text |

---

## Editing protocol

When making changes to pipeline files, follow this order:

1. **Read the file you're about to edit completely.** Pipeline files have load-bearing structural conventions (sign-off blocks, step numbering, sub-section headers). Don't edit blindly.
2. **Check the cross-file consistency table above.** If your change touches a paired file, plan the edit for both at once.
3. **Preserve sign-off blocks.** Each agent ends with a `## ✅ [AGENT] SIGN-OFF` block listing certification items. When adding new validation requirements, add corresponding sign-off items.
4. **Preserve step numbering.** The Editor uses step numbers like 1, 2, 3, 4, 4a, 4.5, 5, 6, 7. New steps insert with sub-numbering (e.g., 4a) rather than renumbering existing steps.
5. **For markdown documents, validate code fence balance after editing** (every ``` must have a closing ```).
6. **Keep each agent's `📂 CONTEXT MANIFEST` in sync with its INPUT section.** Every main agent spec opens with a manifest telling the runtime agent exactly what to load (small-context models depend on it). If your edit changes what an agent reads — a new input file, a new template, a section moved to another file — update the manifest in the same edit.
7. **Use git aggressively.** Commit before architectural changes. Branch for experiments. The pipeline has gotten complex enough that "what did we decide three weeks ago and why" is a real question; git history is the answer.
8. **Record the change in `CHANGELOG.md` — always, in the same change.** Every new feature, new agent/posture/mode, behavior change, or bug fix to the pipeline gets a `CHANGELOG.md` entry. Add it as part of the same edit/commit (and PR) that makes the change — never as an afterthought, and never leave it for "later." Use the existing format: newest entry first, `## YYYY-MM-DD — short title (#PR if there is one)`, a short context paragraph, then `### Added` / `### Changed` / `### Fixed` / `### Removed` sections naming the files touched. Pure typo or wording fixes that don't change how the pipeline behaves don't need an entry; anything that changes behavior, structure, or an agent's contract does. When in doubt, add one.

---

## Common failure modes to avoid

These are mistakes that have been made before and produce subtle bugs:

- **Adding engine instructions to a card template.** The override architecture forbids this. Engine instructions live in the preset Main Prompt.
- **Adding character/world specifics to the preset Main Prompt.** Same architecture, inverse direction. Specifics live in cards and lorebooks.
- **Using lorebook position values without consulting Notes_On_functionality.md.** Position 5 is EMTop (prepended), not "Before Example Messages" or "After Example Dialogue" — verify before writing.
- **Marking an entry "DEFAULT" in Position Rationale when it isn't actually using defaults.** The Editor's 4.5b check catches this; produce honest rationales.
- **Writing audit/recommendation content as if the auditor will apply it.** Auditor agents are read-only on the files they audit. Use "recommend" language consistently.
- **Conflating ARC_STATE descriptive content with directive content.** Use the two-subsection structure; don't merge them. The sandbox `SANDBOX_STATE` entry inherits the same rule (`**Standing Situation:**` descriptive, `**Tonal Mandate:**` directive).
- **Authoring a sandbox NPC roster with overlapping voice fingerprints.** At sandbox scale the failure mode is cross-NPC homogenization, not per-NPC infidelity. Every roster NPC's voice fingerprint must be distinct enough that the Voice Auditor's blind-line test (Step 3I) can tell them apart.
- **Forcing a sandbox world through arc machinery.** Don't author `CHARACTER_STATE`/`NPC_SHIFT`/`DRAMATIC_BEAT`/arc triggers for a `World Mode: sandbox` world — Tier 3 is the single Sandbox lorebook (`SANDBOX_STATE` + `WORLD_PULSE`). Conversely, don't apply the sandbox format to an arc world.
- **Keying lorebook entries by sequential position instead of by UID.** ST stores and looks up world-info entries as `entries[uid]`; an entry keyed `"1"` with `"uid": 20` imports without error and then never renders in the World Info editor (GitHub issue #31). Every entry's object key must equal `String(uid)` — Compiler Foundational Rule 9, deterministically re-checked by `tools/validate_export.py`. The same issue covered the snake_case alias fields (`case_sensitive`, `match_whole_words`, `use_regex`, `characterFilterNames`/`characterFilterExclude`) — those belong to the embedded `character_book` card format, not standalone lorebooks (Rule 10).
- **Keying the NPC Memory Manifest by UID instead of by stable slug.** Memory persistence depends on slug ids derived from the canonical name (principle #12), not UIDs — UIDs renumber on re-export. Each manifest is per-file: its `facets`/`scenes` uids must resolve within its own lorebook (Compiler Step 7.7h), and a lorebook's manifest is regenerated against that file's preserved + new UIDs whenever it is recompiled.
- **Editing `Notes_On_functionality.md` based on memory of how SillyTavern works.** Always verify against the official ST source if changing this file.
- **Adding a new pipeline phase or agent without updating the orchestration** — the router (`workflows/world-forge.md`: overview diagram, DISPATCH PROTOCOL artifact table, trigger commands) *and* the phase's stage file. Orchestration is the source of truth for what runs when.
- **Writing Export JSON through PowerShell (Compiler / mini-Compiler).** Windows PowerShell `Out-File` / `Set-Content` / `>` re-encode to UTF-16 / Windows-1252 and corrupt the em-dashes (—), curly quotes, and accented names that fill lorebook and card content into mojibake (`—` → `â€"`). The mangled file still passes `JSON.parse`, so the "JSON parses" guard never catches it. Write JSON as UTF-8 via the file-write tool directly or a **Python / Node** script, and after writing grep for `â€` / `Ã` (expect zero). See the Compiler's FILE-WRITING & ENCODING guard. `python tools/validate_export.py Export/` checks for exactly this corruption (plus parse, `{{original}}`, and position-enum failures) read-only — run it after every compile.

---

## Working style notes for AI agents

The project owner (Andrei) has a specific working style that produces good outcomes:

- **Decisions before implementation.** When proposing changes, present options with tradeoffs and wait for explicit approval before writing patches. Multi-file changes especially benefit from a "final scope confirmed?" gate.
- **Push back honestly.** When a proposed change would harm the architecture or solve a non-existent problem, say so directly. Several improvements to this pipeline came from agents pushing back on requests that would have introduced complexity without value.
- **Solve actual problems, not anticipated ones.** "Build one manually first, then come back if I find friction points" is the preferred pattern over speculative documentation.
- **Surface latent bugs you notice during other work.** When fixing one thing, if you notice another thing that's wrong, flag it separately rather than fixing it silently or ignoring it.
- **Verify against authoritative sources.** Memory of how SillyTavern works is unreliable; the official source and `Notes_On_functionality.md` are the references.

---

## Out of scope for this repo

Do not, without explicit instruction:

- Modify SillyTavern itself (this repo produces inputs for ST, not changes to ST)
- Add language-specific code (Python, JavaScript, etc.) — the pipeline is pure markdown/JSON. **Two standing exceptions, each explicitly approved:** `tools/validate_export.py` (approved 2026-06-10), a stdlib-only, strictly read-only validator for Export/ JSON (UTF-8/mojibake, parse, `{{original}}`, position enum, UID uniqueness, entry-key/UID parity, snake_case alias fields, leaked inline revision markers), and `tools/validate_pipeline_state.py` (approved 2026-07-30), a stdlib-only, strictly read-only process-state validator that re-checks the Pipeline State Ledger's claims against the artifacts and sign-off anchors actually on disk (the DISPATCH PROTOCOL's deterministic backstop against silently skipped phases). They are deterministic backstops for the Compiler's guards and the orchestrator's gates, not replacements for either. Do not extend them into anything that modifies files, and do not add further scripts without the same explicit approval.
- Add CI/CD, test frameworks, or build configuration — there is nothing to compile or deploy
- Add dependencies of any kind
- Refactor file paths or folder structure (every agent references specific paths)
- Generate world content (worlds are produced by running the pipeline against a user's World Seed; this repo defines the pipeline, not specific worlds — except for `Samples/`)
- Add automated linting that modifies files (validation logic belongs in agent specs, not as separate tooling; the read-only `tools/` validators are the sanctioned exceptions precisely because they modify nothing)

---

## Where to learn more

- **Pipeline overview:** `README.md` (human-facing) and `workflows/world-forge.md` (agent-facing router; per-stage orchestration in `workflows/world-forge-{discovery,drafting,validation,construction}.md`)
- **SillyTavern runtime mechanics:** `Notes_On_functionality.md`
- **Per-phase agent specs:** `agent_roles/*.md`
- **Output structural targets:** `templates/*.md` and `templates/*.json`
- **Sample world outputs:** `Samples/`